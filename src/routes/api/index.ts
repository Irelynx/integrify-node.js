import { Router } from 'express';

// import { router as v1, mountSuffix as mountSuffix_v1 } from "./v1";

const router = Router();

// router.use(mountSuffix_v1, v1);

export { router };
export default router;